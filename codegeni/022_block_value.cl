class Main inherits IO {
    
    main() : Object {
        out_string({
            out_string("este es un bloque de ");
            200;
            "muchos strings";
            3 < 4;
            if false then "palta" else "queso" fi;
            })
    };
};